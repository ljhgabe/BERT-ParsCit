# @package _global_

hydra:
  run:
    dir: /data3/jiahe/logs/debugs/runs/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  sweep:
    dir: /data3/jiahe/logs/debugs/multiruns/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.num}
